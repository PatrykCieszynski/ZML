[project]
name = "zml-workspace"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.uv]
package = false

[tool.uv.sources]
zml-ocr-protocol = { workspace = true }

[tool.uv.workspace]
members = [
    "apps/backend",
    "apps/ocr-worker",
    "packages/ocr-protocol",
]
